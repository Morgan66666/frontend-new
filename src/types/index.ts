export interface Product {
    name: string;
    category: string;
    price: string;
    options: string[];
    description: string;
}

export interface Activity {
    id: string
    name: string;
    description: string;
    location: string;
    date: string;
    img: string;
    price: string;
}